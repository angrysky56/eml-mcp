---
wave: 1
depends_on: []
files_modified:
  - "src/eml_mcp/models/eml_transformer.py"
  - "tests/test_dynamic_topology.py"
autonomous: true
---

# Plan 01: Dynamic Topology Optimization Hooks

## Objective
Implement pruning mechanisms for the EML-Transformer that remove functionally redundant heads during training by hooking into the `EGraph` simplifier system, satisfying requirements OPT-01 and MODEL-07.

## Requirements Addressed
- **OPT-01**: Symbolic Pruning Loop mechanism
- **MODEL-07**: Dynamic network optimization using equality graphs

## Implementation Details

<task type="tdd">
  <description>Create a topology translation utilities module that safely maps active PyTorch EMLCompiledFFN sub-graphs to and from EML-Tree representations, allowing us to compute E-Classes representing forward functions.</description>
  <read_first>
    - "src/eml_mcp/models/eml_transformer.py"
    - "src/eml_mcp/simplifier.py"
  </read_first>
  <action>
    Add `network_to_etree(layer_weights)` and `prune_redundant_features(etree)` methods inside the model definition or a companion utility file. The utility should convert the nested parameters matrix mapping to symbolic `EMLNode`s, feed them into the `simplify_tree()` graph loop, and return the minimal `k` structured graph parameter indices marking which PyTorch weight blocks to drop.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_dynamic_topology.py::test_etree_mapping -v` passes
    - Utility appropriately yields a list of structural indices deemed optimal by `simplify_tree()`.
  </acceptance_criteria>
</task>

<task type="tdd">
  <description>Implement the explicit Pruning Hook injected into the PyTorch Training Loop.</description>
  <read_first>
    - "src/eml_mcp/models/eml_transformer.py"
  </read_first>
  <action>
    Introduce the `apply_symbolic_pruning()` bound instance method to the `EMLTransformer` model instance. This hooks structurally after backpropagation roughly every $N$ epochs. The code should actively zero-out weights (and biases) connected to heads strictly classified outside the optimal topological selection by `prune_redundant_features()`.
  </action>
  <acceptance_criteria>
    - `apply_symbolic_pruning()` successfully removes explicitly constructed redundant heads in the test environment.
    - Forward pass calculations show statistically negligible MSE perturbations post-pruning.
  </acceptance_criteria>
</task>

## Verification
- Core Transformer architectures natively interact with global equality saturation logic to dynamically cull nodes mirroring operations such as `exp(ln(z))`.
- Training dynamics demonstrate sustained and identical precision levels on sparse target benchmarks (like `math.tanh` proxying).

## Must Haves
- The model cleanly intercepts PyTorch matrices, translates to discrete `EML` graphs natively, executes graph extraction via `simplifier.py`, and selectively manipulates backend PyTorch weight sparsity accordingly.
